# github-actions-cloud-iac-pipelines
A reusable DevOps CI/CD platform built with GitHub Actions that automates infrastructure provisioning and application deployment. The pipeline supports Terraform plan/apply workflows, environment approvals, security checks, and promotion across environments using configuration only.
